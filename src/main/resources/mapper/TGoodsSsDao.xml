<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhaishu.qishouserver.entity.dao.TGoodsSsDao">

    <resultMap type="com.zhaishu.qishouserver.entity.TGoodsSs" id="TGoodsSsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="goodsId" column="goods_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="sellPrice" column="sell_price" jdbcType="NUMERIC"/>
        <result property="originalPrice" column="original_price" jdbcType="NUMERIC"/>
        <result property="pictureUrl" column="picture_url" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TGoodsSsMap">
        select
          id, goods_id, name, sell_price, original_price, picture_url, create_time
        from t_goods_ss
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TGoodsSsMap">
        select
          id, goods_id, name, sell_price, original_price, picture_url, create_time
        from t_goods_ss
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="goodsId != null">
                and goods_id = #{goodsId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="sellPrice != null">
                and sell_price = #{sellPrice}
            </if>
            <if test="originalPrice != null">
                and original_price = #{originalPrice}
            </if>
            <if test="pictureUrl != null and pictureUrl != ''">
                and picture_url = #{pictureUrl}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from t_goods_ss
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="goodsId != null">
                and goods_id = #{goodsId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="sellPrice != null">
                and sell_price = #{sellPrice}
            </if>
            <if test="originalPrice != null">
                and original_price = #{originalPrice}
            </if>
            <if test="pictureUrl != null and pictureUrl != ''">
                and picture_url = #{pictureUrl}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into t_goods_ss(goods_id, name, sell_price, original_price, picture_url, create_time)
        values (#{goodsId}, #{name}, #{sellPrice}, #{originalPrice}, #{pictureUrl}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into t_goods_ss(goods_id, name, sell_price, original_price, picture_url, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.goodsId}, #{entity.name}, #{entity.sellPrice}, #{entity.originalPrice}, #{entity.pictureUrl}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into t_goods_ss(goods_id, name, sell_price, original_price, picture_url, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.goodsId}, #{entity.name}, #{entity.sellPrice}, #{entity.originalPrice}, #{entity.pictureUrl}, #{entity.createTime})
        </foreach>
        on duplicate key update
        goods_id = values(goods_id),
        name = values(name),
        sell_price = values(sell_price),
        original_price = values(original_price),
        picture_url = values(picture_url),
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update t_goods_ss
        <set>
            <if test="goodsId != null">
                goods_id = #{goodsId},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="sellPrice != null">
                sell_price = #{sellPrice},
            </if>
            <if test="originalPrice != null">
                original_price = #{originalPrice},
            </if>
            <if test="pictureUrl != null and pictureUrl != ''">
                picture_url = #{pictureUrl},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from t_goods_ss where id = #{id}
    </delete>

</mapper>

